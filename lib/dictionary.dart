import 'package:flutter/material.dart';
class DictionaryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("DictionaryPage"),//todo dynamically update
      ),
    );
  }
}